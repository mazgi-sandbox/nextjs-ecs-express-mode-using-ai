# Prompts to grow step-5 into step-6

- Add a User Prisma model under `backend/prisma/user.prisma` with fields: id (cuid), email (unique), name, passwordHash, totpSecret, totpEnabled, recoveryCodes, preferences, createdAt, updatedAt, and a relation to items (one-to-many). Update the Item model to add a userId field and a User relation. Add a userId parameter to the items service create/findAll/remove methods and protect items by user ownership.
- Create a Users module under `backend/src/users/` with: `users.module.ts`, `users.controller.ts` (CRUD + preferences endpoint), `users.service.ts` (getMe returning user without sensitive fields, create, findAll, findOne, update, updatePreferences, remove), DTOs (`create-user.dto.ts`, `update-user.dto.ts`), and entity (`user.entity.ts`). Export UsersService from UsersModule.
- Create an Auth module under `backend/src/auth/` with email/password authentication (no email verification, no password reset). Include: `auth.module.ts` (imports PrismaModule, UsersModule, PassportModule, JwtModule), `auth.service.ts` (signUp creating user with hashed password and returning tokens immediately, signIn validating credentials, refresh, deleteAccount, buildTokenResponse helper), `auth.controller.ts` (POST /signup, /signin, /refresh, GET /me, DELETE /account), DTOs (`signup.dto.ts`, `signin.dto.ts`, `refresh.dto.ts`), `guards/jwt-auth.guard.ts`, `strategies/jwt.strategy.ts`. Add `@nestjs/jwt`, `@nestjs/passport`, `passport`, `passport-jwt`, `bcryptjs`, `jsonwebtoken`, `express-session`, `connect-pg-simple`, `otpauth` and their type definitions to package.json.
- Add TOTP MFA to the auth module. Include: `totp/setup` (generate secret + QR URI), `totp/enable` (verify code, return recovery codes), `totp/disable`, `totp/verify` (MFA challenge during signin), `totp/recovery-codes` (regenerate). Add DTOs (`totp-code.dto.ts`, `totp-verify.dto.ts`). When TOTP is enabled, signIn should return `{ requiresMfa: true, mfaToken }` instead of tokens.
- Add i18n support to the backend using `nestjs-i18n`. Create `src/i18n/en/` and `src/i18n/ja/` directories with JSON files for auth, items, users, and validation messages. Configure I18nModule in AppModule with fallback language 'en', QueryResolver, and AcceptLanguageResolver. Update nest-cli.json to include i18n assets.
- Protect the items controller with `JwtAuthGuard`. Items should belong to the authenticated user — create/findAll/remove should use `req.user.userId`. Update items service to filter by userId.
- Create the web frontend auth system: `contexts/AuthContext.tsx` (manages user state, accessToken, refreshToken with auto-refresh), `contexts/ThemeContext.tsx`, `contexts/LocaleContext.tsx`, `app/providers.tsx` (wraps children in all context providers), and update `app/layout.tsx` to use Providers. Add `next-intl` to dependencies. Create `messages/en.json` and `messages/ja.json` with i18n strings for all pages.
- Create web auth pages: `app/signin/page.tsx` (email/password form with TOTP MFA handling), `app/signup/page.tsx` (registration form), `app/dashboard/page.tsx` (authenticated landing page). Create reusable components: `components/AppHeader.tsx` (navigation with sign out), `components/LanguageSwitcher.tsx`, `components/PasswordInput.tsx`. Add `styles/globals.css` and `global.d.ts`.
- Create `app/settings/page.tsx` with theme selector, TOTP MFA setup/enable/disable/regenerate recovery codes, and account deletion. Create `lib/api.ts` with typed API client functions for all auth, user, items, and TOTP endpoints (using `/backend` proxy prefix).
- Update the items page to require authentication — redirect to /signin if not logged in, use accessToken from AuthContext for API calls, and display items owned by the authenticated user.
- Add `app/page.tsx` as a redirect page: if authenticated go to /dashboard, otherwise go to /signin.
- Add e2e tests: `web/e2e-tests/tests/auth.spec.ts` (signup, signin, signout flows) and `web/e2e-tests/tests/git-sha.spec.ts` (verify git SHA overlay). Update web next.config.js to add BACKEND_URL rewrite proxy and GIT_SHA env injection.
