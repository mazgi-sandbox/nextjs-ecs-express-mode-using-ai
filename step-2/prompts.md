# Prompts to grow step-2 into step-3

- Add an IaC Dockerfile at Dockerfiles.d/iac/Dockerfile based on `hashicorp/terraform:latest`. Install `shadow` and `su-exec` via apk. Create a developer user matching host UID/GID. Add an entrypoint that copies `~/.aws` from read-only bind mount to the developer user's home with correct ownership, then runs the command as the developer user via `su-exec`.
- Add Terraform IaC for AWS under iac/aws/ with a single flat layer (no persistent/ephemeral split). Include: VPC with public and private subnets across 2 AZs, internet gateway, NAT gateway, ECR repository for the web image, IAM roles for ECS task execution and ECS Express infrastructure, security group for web (port 3000), and an ECS Express gateway service for the web app. Use S3 backend for state with `-backend-config` at init time. Variables: `app_unique_id`, `aws_region`, `image_tag`.
- Add a bootstrap-tfstate-s3 GitHub Action under .github/actions/bootstrap-tfstate-s3/ that creates an S3 bucket and enables versioning if it doesn't exist.
- Add an `iac` service to compose.yaml that builds from Dockerfiles.d/iac/Dockerfile, mounts `~/.aws` read-only, passes `AWS_PROFILE`, `TF_VAR_app_unique_id`, `TF_VAR_aws_region`, and `TF_VAR_image_tag` as environment variables, and runs under the `iac` profile.
- Add GitHub Actions workflows: `iac.yaml` (triggers on iac/aws changes, PR=plan, push to main=apply), `_reusable-iac.yaml` (reusable workflow with AWS OIDC auth, S3 backend bootstrap, terraform init/plan/apply), `_reusable-web-build.yaml` (build and push web image to GHCR and optionally ECR), `web.build.yaml` (triggers on web/app or iac changes), and `web.e2e-tests.yaml` (runs Playwright e2e tests via docker compose).
- Add docs: `docs/cloud-deployment.md` (overview of Terraform commands), `docs/cloud-deployment-aws.md` (AWS-specific deployment guide with prerequisites, state bucket setup, variable configuration, deploy/teardown steps, and resources created table), `docs/ci.md` (workflows overview, env file naming, required variables), and `docs/oidc-setup.md` (one-time AWS IAM OIDC provider and role setup).
- Update .example.env to add AWS and IaC variables: `AWS_PROFILE`, `AWS_IAM_ROLE_ARN`, `AWS_TF_STATE_BUCKET`, `AWS_TF_STATE_REGION`, `AWS_REGION`, `APP_UNIQUE_ID`, `IMAGE_TAG`.
- Update README.md to reflect step-3 scope: web + e2e tests + IaC + CI/CD.
