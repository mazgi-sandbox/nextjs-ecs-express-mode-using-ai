# Prompts to grow step-6 into step-99

- Add email verification to the signup flow. Add `emailVerified` (boolean, default false), `emailVerificationToken` (unique string), and `emailVerificationExpires` (DateTime) fields to the User Prisma model. Update `auth.service.ts` signUp to generate a verification token, store it with a 24h expiry, and send a verification email instead of returning tokens immediately. Add a `POST /auth/verify-email` endpoint that validates the token, marks the email as verified, and returns a success message. Add a `POST /auth/resend-verification` endpoint. Update signIn to reject users with unverified emails. Add DTOs: `resend-verification.dto.ts`.
- Add password reset flow. Add `passwordResetToken` (unique string) and `passwordResetExpires` (DateTime) fields to the User model. Add `POST /auth/forgot-password` (generates token, sends reset email) and `POST /auth/reset-password` (validates token, updates password) endpoints with DTOs: `forgot-password.dto.ts`, `reset-password.dto.ts`. The forgot-password endpoint should always return success (to prevent email enumeration).
- Add email update. Add `PATCH /auth/email` endpoint that changes the user's email, resets emailVerified to false, generates a new verification token, and sends a verification email. Add `update-email.dto.ts` DTO.
- Create a Mail module under `backend/src/mail/` with `mail.module.ts` and `mail.service.ts`. Use nodemailer to send emails. Configure SMTP from environment variables (SMTP_HOST, SMTP_PORT, SMTP_SECURE, SMTP_USER, SMTP_PASS, SMTP_FROM). Implement methods for sending verification emails and password reset emails with HTML templates. Add `nodemailer` and `@types/nodemailer` to package.json. Import MailModule in AuthModule.
- Add a Mailpit service to compose.yaml for local email testing (SMTP on port 1025, web UI on port 8025). Make the backend depend on mailpit. Add SMTP environment variables to .example.env (SMTP_HOST=mailpit, SMTP_PORT=1025, SMTP_SECURE=false, SMTP_FROM=noreply@localhost). Add SMTP_PASS to .example.secrets.env.
- Add SMTP infrastructure to Terraform: add SMTP variables (smtp_host, smtp_port, smtp_secure, smtp_user, smtp_from) to iac/aws/variables.tf. Add SMTP environment blocks to ecs-express-backend.tf. Add SMTP_PASS secret to secrets-manager.tf and the IAM policy. Add SMTP TF_VARs to compose.yaml iac service and _reusable-iac.yaml workflow.
- Create a verify-email page at `web/app/app/verify-email/page.tsx` that reads a token from the URL search params, calls the verify-email API, and shows success or error. Wrap with Suspense boundary for useSearchParams.
- Create a reset-password page at `web/app/app/reset-password/page.tsx` that reads a token from URL params, shows a new password form, and calls the reset-password API. Wrap with Suspense boundary.
- Add forgot password to the signin page: add a "Forgot password?" button that calls the forgot-password API with the entered email and shows a success message. Add i18n strings for forgotPasswordLink, forgotPasswordSending, forgotPasswordSent, forgotPasswordEnterEmail.
- Add email management to the settings page: display the current email with verified/unverified badge, a resend verification button, and an email update form. Add i18n strings for email-related settings keys.
- Update the signup page to show a "Check your email" verification message after successful registration, with a resend verification button. Add VerifyEmail and ResetPassword sections to i18n message files (en.json, ja.json).
- Add backend scripts for express-session: `scripts/session-table.sql` (PostgreSQL session table DDL) and `scripts/apply-session-table.mjs` (applies the SQL via pg client). These support the connect-pg-simple session store used for OAuth session state.
- Add comprehensive e2e tests: `backend/test/auth.e2e-spec.ts` (signup, verify-email, signin, refresh, forgot/reset password), `backend/test/totp.e2e-spec.ts` (TOTP setup/enable/disable/verify), `backend/test/users.e2e-spec.ts` (user CRUD), `backend/test/preferences.e2e-spec.ts` (user preferences), `backend/test/helpers.ts` (shared test helpers for creating verified users). Add `web/e2e-tests/tests/theme.spec.ts` (theme switching) and `web/e2e-tests/tests/totp.spec.ts` (TOTP MFA flow).
